# -*- coding: utf-8 -*-
"""
通用配置文件

所有可调整的游戏参数集中管理
"""

# ============================================================
# 喵币自动获取配置
# ============================================================
COIN_AUTO_INTERVAL = 300       # 自动获取间隔（秒），默认5分钟
COIN_AUTO_AMOUNT = 5           # 每次自动获取数量
COIN_MAX_OFFLINE_EARN = 100    # 离线期间最多获取喵币上限（防止过量）

# ============================================================
# 桌宠属性配置
# ============================================================
# 属性初始值
PET_INIT_SATIETY = 100         # 初始饱食度
PET_INIT_THIRST = 100          # 初始饥渴值
PET_INIT_MOOD = 100            # 初始心情
PET_INIT_EXP = 0               # 初始经验值

# 属性自然衰减（每秒减少量）
PET_DECAY_SATIETY = 0.02       # 饱食度每秒衰减
PET_DECAY_THIRST = 0.02        # 饥渴值每秒衰减
PET_DECAY_MOOD = 0.015         # 心情每秒衰减

# 属性上下限
PET_ATTR_MIN = 0
PET_ATTR_MAX = 100

# ============================================================
# 经验值联动配置
# ============================================================
# 三个属性（饱食度、饥渴值、心情）联动影响经验值
# 格式: (属性最低值, 判定间隔秒, 每次经验变化量)
EXP_THRESHOLDS = [
    # (属性阈值, 判定间隔秒, 经验变化)
    (100, 120, 3),     # 三个属性都=100，每2分钟+3经验
    (80,  180, 2),     # 三个属性都>=80，每3分钟+2经验
    (60,  300, 1),     # 三个属性都>=60，每5分钟+1经验
    (60,  300, -1),    # 三个属性都<60，每5分钟-1经验
    (40,  240, -2),    # 三个属性都<40，每4分钟-2经验
    (20,  180, -3),    # 三个属性都<20，每3分钟-3经验
    (0,   120, -5),    # 三个属性都=0，每2分钟-5经验
]

# 经验值上下限
EXP_MIN = 0
EXP_MAX = 999999

# ============================================================
# 等级系统配置
# ============================================================
# 每级所需总经验（从1级开始）
# 等级 = 总经验 / EXP_PER_LEVEL 的整数部分
# 当前等级经验 = 总经验 % EXP_PER_LEVEL
EXP_PER_LEVEL = 100            # 每级所需经验

# ============================================================
# 物品系统配置
# ============================================================
# 物品类型
ITEM_TYPE_FOOD = "food"           # 食物：影响饱食度
ITEM_TYPE_DRINK = "drink"         # 饮品：影响饥渴值
ITEM_TYPE_TOY = "toy"             # 玩具：影响心情
ITEM_TYPE_DECORATION = "decoration"  # 装饰：仅装饰

# 物品保护效果（使用期间对应属性不衰减）
ITEM_PROTECTION_ENABLED = True

# 装饰品配置
DECORATION_SLOTS = {
    "head": "头部",
    "face": "面部",
    "body": "身体",
    "accessory": "配饰",
}